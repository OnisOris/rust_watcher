# UI Screenshot Review (before)

Captured from `./example` at 2026-06-21T10:34:37.310Z.

## Matrix

- Layouts: Semantic zones, Package map, Local neighborhood
- Modes: Macro, Meso, Micro, Call Flow, Data Flow, Types & Impl
- Viewports: 1600x900, 1920x1080

## Console

- No browser console errors captured.

## Visual notes

- `example-package-macro-1600.png`: API Boundary is still a tall mostly-empty red panel. Route cards sit in the upper half instead of a compact centered gateway.
- `example-package-macro-1600.png`: legend is centered under the filter bar and visually competes with filters/top controls.
- `example-package-macro-1600.png`: package cards have useful structure, but the text is still tiny and looks like debug details instead of overview cards.
- `example-semantic-meso-1600.png`: Semantic zones are readable but dense. API edges are primary, but the boundary panel still consumes too much vertical space.
- `example-neighborhood-macro-1600.png`: LocalNeighborhood correctly shows only a guide, but the guide text is duplicated by the normal node label underneath the card.
